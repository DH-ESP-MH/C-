#include <cstdio>
#include <iostream>
#include <math.h>

int main() {
  double PI = 2 * acos(0.0);
  int a = 3;
  double b = 0.521;
  std::cout << cbrt(
      pow((0, 127 * exp(b / a)) / 1 - cbrt(cos((b / a) * PI)), 2));
}